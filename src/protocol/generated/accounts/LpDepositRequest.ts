/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita 
 */

import * as web3 from '@solana/web3.js';
import * as beet from '@metaplex-foundation/beet';
import * as beetSolana from '@metaplex-foundation/beet-solana';




/**
 * Arguments used to create {@link LpDepositRequest}
 * @category Accounts
 * @category generated
 */
export type LpDepositRequestArgs = {
  tag: number
  bump: number
  status: number
  hdrPad: number[] /* size: 5 */
  pool: web3.PublicKey
  user: web3.PublicKey
  userLpShielded: web3.PublicKey
  requestCounter: beet.bignum
  amountA: beet.bignum
  amountB: beet.bignum
  minLpOut: beet.bignum
  submittedSlot: beet.bignum
  reserved: number[] /* size: 32 */
}

;
/**
 * Holds the data for the {@link LpDepositRequest} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class LpDepositRequest implements LpDepositRequestArgs {
  readonly tag: number;
  readonly bump: number;
  readonly status: number;
  readonly hdrPad: number[] /* size: 5 */;
  readonly pool: web3.PublicKey;
  readonly user: web3.PublicKey;
  readonly userLpShielded: web3.PublicKey;
  readonly requestCounter: beet.bignum;
  readonly amountA: beet.bignum;
  readonly amountB: beet.bignum;
  readonly minLpOut: beet.bignum;
  readonly submittedSlot: beet.bignum;
  readonly reserved: number[] /* size: 32 */;
  private constructor(
    tag: number,
    bump: number,
    status: number,
    hdrPad: number[] /* size: 5 */,
    pool: web3.PublicKey,
    user: web3.PublicKey,
    userLpShielded: web3.PublicKey,
    requestCounter: beet.bignum,
    amountA: beet.bignum,
    amountB: beet.bignum,
    minLpOut: beet.bignum,
    submittedSlot: beet.bignum,
    reserved: number[] /* size: 32 */
  ) {
    this.tag = tag;
    this.bump = bump;
    this.status = status;
    this.hdrPad = hdrPad;
    this.pool = pool;
    this.user = user;
    this.userLpShielded = userLpShielded;
    this.requestCounter = requestCounter;
    this.amountA = amountA;
    this.amountB = amountB;
    this.minLpOut = minLpOut;
    this.submittedSlot = submittedSlot;
    this.reserved = reserved;
  }

  /**
   * Creates a {@link LpDepositRequest} instance from the provided args.
   */
  static fromArgs(args: LpDepositRequestArgs) {
    return new LpDepositRequest(
      args.tag,
      args.bump,
      args.status,
      args.hdrPad,
      args.pool,
      args.user,
      args.userLpShielded,
      args.requestCounter,
      args.amountA,
      args.amountB,
      args.minLpOut,
      args.submittedSlot,
      args.reserved
    );
  }

  /**
   * Deserializes the {@link LpDepositRequest} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [ LpDepositRequest, number ]  {
    return LpDepositRequest.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link LpDepositRequest} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig,
  ): Promise<LpDepositRequest> {
    const accountInfo = await connection.getAccountInfo(address, commitmentOrConfig);
    if (accountInfo == null) {
      throw new Error(`Unable to find LpDepositRequest account at ${address}`);
    }
    return LpDepositRequest.fromAccountInfo(accountInfo, 0)[0];
  }


  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId: web3.PublicKey = new web3.PublicKey('4vTNEf7bpNoYXQJPtQNFYaRxJ8tnvNmSwHW6iAUcz4Gq')) {
    return beetSolana.GpaBuilder.fromStruct(programId, lpDepositRequestBeet)
  }

  /**
   * Deserializes the {@link LpDepositRequest} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(
    buf: Buffer,
    offset = 0
  ): [ LpDepositRequest, number ]{
    return lpDepositRequestBeet.deserialize(buf, offset);
  }

  /**
   * Serializes the {@link LpDepositRequest} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [ Buffer, number ] {
    return lpDepositRequestBeet.serialize(this)
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link LpDepositRequest}
   */
  static get byteSize() {
    return lpDepositRequestBeet.byteSize;
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding 
   * {@link LpDepositRequest} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment,
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      LpDepositRequest.byteSize,
      commitment,
    );
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link LpDepositRequest} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === LpDepositRequest.byteSize;
  }

  /**
   * Returns a readable version of {@link LpDepositRequest} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      tag: this.tag,
      bump: this.bump,
      status: this.status,
      hdrPad: this.hdrPad,
      pool: this.pool.toBase58(),
      user: this.user.toBase58(),
      userLpShielded: this.userLpShielded.toBase58(),
      requestCounter: (() => {
        const x = (this.requestCounter as { toNumber: () => number })
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) { return x }
        }
        return x
      })(),
      amountA: (() => {
        const x = (this.amountA as { toNumber: () => number })
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) { return x }
        }
        return x
      })(),
      amountB: (() => {
        const x = (this.amountB as { toNumber: () => number })
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) { return x }
        }
        return x
      })(),
      minLpOut: (() => {
        const x = (this.minLpOut as { toNumber: () => number })
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) { return x }
        }
        return x
      })(),
      submittedSlot: (() => {
        const x = (this.submittedSlot as { toNumber: () => number })
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) { return x }
        }
        return x
      })(),
      reserved: this.reserved
    };
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const lpDepositRequestBeet = new beet.BeetStruct<
    LpDepositRequest,
    LpDepositRequestArgs
>(
  [
    
    ['tag', beet.u8],
    ['bump', beet.u8],
    ['status', beet.u8],
    ['hdrPad', beet.uniformFixedSizeArray(beet.u8, 5)],
    ['pool', beetSolana.publicKey],
    ['user', beetSolana.publicKey],
    ['userLpShielded', beetSolana.publicKey],
    ['requestCounter', beet.u64],
    ['amountA', beet.u64],
    ['amountB', beet.u64],
    ['minLpOut', beet.u64],
    ['submittedSlot', beet.u64],
    ['reserved', beet.uniformFixedSizeArray(beet.u8, 32)]
  ],
  LpDepositRequest.fromArgs,
  'LpDepositRequest'
)