/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita 
 */

import * as web3 from '@solana/web3.js';
import * as beet from '@metaplex-foundation/beet';
import * as beetSolana from '@metaplex-foundation/beet-solana';




/**
 * Arguments used to create {@link OrderTicket}
 * @category Accounts
 * @category generated
 */
export type OrderTicketArgs = {
  tag: number
  bump: number
  status: number
  side: number
  hdrPad: number[] /* size: 4 */
  pool: web3.PublicKey
  owner: web3.PublicKey
  userTokenAccount: web3.PublicKey
  batchId: beet.bignum
  submittedSlot: beet.bignum
  inputCiphertext: number[] /* size: 128 */
  reserved: number[] /* size: 16 */
}

;
/**
 * Holds the data for the {@link OrderTicket} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class OrderTicket implements OrderTicketArgs {
  readonly tag: number;
  readonly bump: number;
  readonly status: number;
  readonly side: number;
  readonly hdrPad: number[] /* size: 4 */;
  readonly pool: web3.PublicKey;
  readonly owner: web3.PublicKey;
  readonly userTokenAccount: web3.PublicKey;
  readonly batchId: beet.bignum;
  readonly submittedSlot: beet.bignum;
  readonly inputCiphertext: number[] /* size: 128 */;
  readonly reserved: number[] /* size: 16 */;
  private constructor(
    tag: number,
    bump: number,
    status: number,
    side: number,
    hdrPad: number[] /* size: 4 */,
    pool: web3.PublicKey,
    owner: web3.PublicKey,
    userTokenAccount: web3.PublicKey,
    batchId: beet.bignum,
    submittedSlot: beet.bignum,
    inputCiphertext: number[] /* size: 128 */,
    reserved: number[] /* size: 16 */
  ) {
    this.tag = tag;
    this.bump = bump;
    this.status = status;
    this.side = side;
    this.hdrPad = hdrPad;
    this.pool = pool;
    this.owner = owner;
    this.userTokenAccount = userTokenAccount;
    this.batchId = batchId;
    this.submittedSlot = submittedSlot;
    this.inputCiphertext = inputCiphertext;
    this.reserved = reserved;
  }

  /**
   * Creates a {@link OrderTicket} instance from the provided args.
   */
  static fromArgs(args: OrderTicketArgs) {
    return new OrderTicket(
      args.tag,
      args.bump,
      args.status,
      args.side,
      args.hdrPad,
      args.pool,
      args.owner,
      args.userTokenAccount,
      args.batchId,
      args.submittedSlot,
      args.inputCiphertext,
      args.reserved
    );
  }

  /**
   * Deserializes the {@link OrderTicket} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [ OrderTicket, number ]  {
    return OrderTicket.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link OrderTicket} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig,
  ): Promise<OrderTicket> {
    const accountInfo = await connection.getAccountInfo(address, commitmentOrConfig);
    if (accountInfo == null) {
      throw new Error(`Unable to find OrderTicket account at ${address}`);
    }
    return OrderTicket.fromAccountInfo(accountInfo, 0)[0];
  }


  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(programId: web3.PublicKey = new web3.PublicKey('4vTNEf7bpNoYXQJPtQNFYaRxJ8tnvNmSwHW6iAUcz4Gq')) {
    return beetSolana.GpaBuilder.fromStruct(programId, orderTicketBeet)
  }

  /**
   * Deserializes the {@link OrderTicket} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(
    buf: Buffer,
    offset = 0
  ): [ OrderTicket, number ]{
    return orderTicketBeet.deserialize(buf, offset);
  }

  /**
   * Serializes the {@link OrderTicket} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [ Buffer, number ] {
    return orderTicketBeet.serialize(this)
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link OrderTicket}
   */
  static get byteSize() {
    return orderTicketBeet.byteSize;
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding 
   * {@link OrderTicket} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment,
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      OrderTicket.byteSize,
      commitment,
    );
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link OrderTicket} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === OrderTicket.byteSize;
  }

  /**
   * Returns a readable version of {@link OrderTicket} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      tag: this.tag,
      bump: this.bump,
      status: this.status,
      side: this.side,
      hdrPad: this.hdrPad,
      pool: this.pool.toBase58(),
      owner: this.owner.toBase58(),
      userTokenAccount: this.userTokenAccount.toBase58(),
      batchId: (() => {
        const x = (this.batchId as { toNumber: () => number })
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) { return x }
        }
        return x
      })(),
      submittedSlot: (() => {
        const x = (this.submittedSlot as { toNumber: () => number })
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) { return x }
        }
        return x
      })(),
      inputCiphertext: this.inputCiphertext,
      reserved: this.reserved
    };
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const orderTicketBeet = new beet.BeetStruct<
    OrderTicket,
    OrderTicketArgs
>(
  [
    
    ['tag', beet.u8],
    ['bump', beet.u8],
    ['status', beet.u8],
    ['side', beet.u8],
    ['hdrPad', beet.uniformFixedSizeArray(beet.u8, 4)],
    ['pool', beetSolana.publicKey],
    ['owner', beetSolana.publicKey],
    ['userTokenAccount', beetSolana.publicKey],
    ['batchId', beet.u64],
    ['submittedSlot', beet.u64],
    ['inputCiphertext', beet.uniformFixedSizeArray(beet.u8, 128)],
    ['reserved', beet.uniformFixedSizeArray(beet.u8, 16)]
  ],
  OrderTicket.fromArgs,
  'OrderTicket'
)